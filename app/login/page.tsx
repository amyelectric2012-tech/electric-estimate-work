"use client";import {useState} from "react";import {createClient} from "@supabase/supabase-js";export default function Login(){const [email,setEmail]=useState(""),[message,setMessage]=useState("");async function submit(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("Configure Supabase environment variables first.");return}const s=createClient(url,key);const {error}=await s.auth.signInWithOtp({email});setMessage(error?.message||"Check your email for the sign-in link.");}return <main className="main" style={{marginLeft:0,width:"100%",maxWidth:560}}><div className="hero"><span className="eyebrow">Secure access</span><h1>Contractor sign in</h1><p>Passwordless email sign-in via Supabase.</p></div><section className="card form"><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div><button className="btn" onClick={submit}>Send sign-in link</button>{message&&<p className="note">{message}</p>}</section></main>}