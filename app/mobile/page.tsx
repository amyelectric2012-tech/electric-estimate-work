"use client";import {useState} from "react";export default function Mobile(){const [done,setDone]=useState<string[]>([]);const items=["Verify service rating and panel space","Photograph existing panel and labels","Measure EV route and trench length","Confirm attic/crawl access","Record fixture/receptacle counts","Confirm permit/AHJ scope"];return <main className="main" style={{marginLeft:0,width:"100%",maxWidth:700}}><div className="hero"><span className="eyebrow">Field mode</span><h1>Site visit checklist</h1><p>Large touch targets for phone use. Capture facts before finalizing the estimate.</p></div><div className="card formgrid">{items.map(x=><label className="check" key={x}><input type="checkbox" checked={done.includes(x)} onChange={e=>setDone(e.target.checked?[...done,x]:done.filter(y=>y!==x))}/>{x}</label>)}<div className="note">{done.length}/{items.length} complete</div></div></main>}