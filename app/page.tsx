import Link from 'next/link';import {clients} from '../lib/data';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function Home(){const pipeline=196250;return <div className="shell"><aside className="sidebar"><div className="brand">Electric Estimate Work<small>Los Angeles contractor edition</small></div><nav className="nav"><Link href="/">Dashboard</Link><Link href="/clients">Clients</Link><Link href="/estimates">New Estimate</Link><Link href="/materials">Materials</Link><Link href="/references">NEC References</Link></nav></aside><main className="main"><div className="topbar"><div><div className="eyebrow">Estimating workspace</div><h1 className="title">Dashboard</h1></div><Link className="btn" href="/estimates">+ New estimate</Link></div><div className="grid grid4"><div className="card"><div className="muted">Open estimates</div><div className="metric">14</div></div><div className="card"><div className="muted">Pipeline</div><div className="metric">{'$'}{pipeline.toLocaleString()}</div></div><div className="card"><div className="muted">Active clients</div><div className="metric">{clients.length}</div></div><div className="card"><div className="muted">Avg. margin</div><div className="metric">24.8%</div></div></div><div className="section grid grid2"><div className="card"><h2>Recent clients</h2><table className="table"><thead><tr><th>Client</th><th>Jobs</th><th>Value</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td>{c.name}<div className="note">{c.contact}</div></td><td>{c.jobs}</td><td>{money(c.value)}</td></tr>)}</tbody></table></div><div className="card"><h2>Estimator notes</h2><p className="note">Prices and labor rates are editable starting points, not live supplier quotes. Permit fees and adopted-code requirements vary by jurisdiction and project scope.</p><p className="note">NEC entries here are references and field notes only; the application does not reproduce copyrighted NEC text.</p></div></div></main></div>}
