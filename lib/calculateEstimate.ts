export type EstimateInput={squareFeet:number;fixtures:number;receptacles:number;panels:number;laborHours:number;materialCost:number;permitCost:number;laborRate:number;overheadPct:number;profitPct:number};
export function calculateEstimate(i:EstimateInput){const labor=i.laborHours*i.laborRate;const direct=labor+i.materialCost+i.permitCost;const overhead=direct*(i.overheadPct/100);const subtotal=direct+overhead;const profit=subtotal*(i.profitPct/100);return {labor,direct,overhead,subtotal,profit,total:subtotal+profit};}
